name: Docker Build
'on':
  - push
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v3
      - name: Build Docker image
        run: 'docker build -t myapp:latest .'
